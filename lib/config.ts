// Runtime configuration boundary (re-exported from @lp/config) + DB-backed settings (re-exported from ./settings).
// Both re-exported for backward compatibility — no behavior change for existing importers.

export * from '@lp/config'
export * from './settings'