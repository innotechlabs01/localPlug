{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "rm -rf .next && pnpm build",
  "crons": [
    {
      "path": "/api/cron/process-queue",
      "schedule": "0 6 * * *"
    }
  ]
}
